# 前端架构设计文档

> MelodyClaw 前端页面、功能模块、API交互详细设计

## 1. 页面结构

### 1.1 页面列表

| 页面 | 路由 | 描述 |
|------|------|------|
| 首页 | `/` | 小龙虾播放器 + 快速入口 |
| 歌曲管理 | `/songs` | 上传、管理歌曲 |
| 歌词编辑 | `/lyrics/:id` | 歌词上传、时间对齐 |
| 克隆任务 | `/clone` | 创建克隆任务、查看进度 |
| 结果展示 | `/result/:id` | 播放克隆结果、下载 |
| 音色管理 | `/voices` | 管理音色模型 |

### 1.2 页面导航结构

```
┌─────────────────────────────────────────────────────────┐
│  Header (导航栏)                                         │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐              │
│  │首页 │ │歌曲 │ │克隆 │ │音色 │ │设置 │              │
│  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘              │
└─────────────────────────────────────────────────────────┘
```

---

## 2. 各页面详细设计

### 2.1 首页 (`/`)

#### 功能模块

```
┌─────────────────────────────────────────────────────────┐
│                      首页布局                            │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │              小龙虾播放器                         │   │
│  │  ┌─────────────────────────────────────────┐    │   │
│  │  │         歌词滚动区域                      │    │   │
│  │  │    [上一句]                              │    │   │
│  │  │    [当前歌词 - 高亮放大]                  │    │   │
│  │  │    [下一句]                              │    │   │
│  │  └─────────────────────────────────────────┘    │   │
│  │  ┌─────────────────────────────────────────┐    │   │
│  │  │         🦞 小龙虾动画                    │    │   │
│  │  │     (唱歌时钳子摆动、气泡音符飘出)        │    │   │
│  │  └─────────────────────────────────────────┘    │   │
│  │  ┌─────────────────────────────────────────┐    │   │
│  │  │ [▶播放] [⏸暂停] [⏹停止] ━━━●━━━ 🔊    │    │   │
│  │  └─────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐       │
│  │  快速上传   │ │  创建任务   │ │  查看结果   │       │
│  │  上传歌曲   │ │  开始克隆   │ │  下载歌曲   │       │
│  └─────────────┘ └─────────────┘ └─────────────┘       │
└─────────────────────────────────────────────────────────┘
```

#### API 交互

| 操作 | API | 方法 | 说明 |
|------|-----|------|------|
| 获取最近歌曲 | `/api/v1/songs?limit=5` | GET | 首页展示 |
| 获取播放URL | `/api/v1/songs/{id}/play` | GET | 获取音频流 |
| 获取歌词 | `/api/v1/lyrics/{song_id}` | GET | 歌词数据 |

---

### 2.2 歌曲管理 (`/songs`)

#### 功能模块

```
┌─────────────────────────────────────────────────────────┐
│                      歌曲管理                            │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  [📤 上传歌曲]  [🔍 搜索]  [筛选: 全部/已处理]   │   │
│  └─────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  歌曲列表                                        │   │
│  │  ┌─────┬──────────┬──────┬───────┬─────────┐   │   │
│  │  │封面 │ 歌曲名    │时长  │状态   │操作     │   │   │
│  │  ├─────┼──────────┼──────┼───────┼─────────┤   │   │
│  │  │ 🎵 │ 歌曲A     │ 3:45 │已完成 │▶ 📝 🗑️ │   │   │
│  │  │ 🎵 │ 歌曲B     │ 4:12 │处理中 │⏳      │   │   │
│  │  │ 🎵 │ 歌曲C     │ 2:58 │未处理 │🚀 🗑️   │   │   │
│  │  └─────┴──────────┴──────┴───────┴─────────┘   │   │
│  └─────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  分页: < 1 2 3 ... 10 >                         │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

#### 上传弹窗

```
┌─────────────────────────────────────────┐
│  上传歌曲                          [×]  │
├─────────────────────────────────────────┤
│  歌曲文件                               │
│  ┌───────────────────────────────────┐ │
│  │  拖拽文件或点击选择                │ │
│  │  支持 MP3, WAV, FLAC (最大50MB)   │ │
│  └───────────────────────────────────┘ │
│                                         │
│  歌词文件 (可选)                        │
│  ┌───────────────────────────────────┐ │
│  │  支持 LRC 格式                     │ │
│  └───────────────────────────────────┘ │
│                                         │
│  歌曲信息                               │
│  歌曲名: [________________________]     │
│  歌手:   [________________________]     │
│                                         │
│         [取消]  [上传]                  │
└─────────────────────────────────────────┘
```

#### API 交互

| 操作 | API | 方法 | 说明 |
|------|-----|------|------|
| 获取歌曲列表 | `/api/v1/songs` | GET | 分页查询 |
| 上传歌曲 | `/api/v1/songs` | POST | multipart/form-data |
| 删除歌曲 | `/api/v1/songs/{id}` | DELETE | 删除歌曲及关联文件 |
| 获取歌曲详情 | `/api/v1/songs/{id}` | GET | 歌曲信息 |
| 触发人声分离 | `/api/v1/songs/{id}/separate` | POST | 调用分离API |

---

### 2.3 歌词编辑 (`/lyrics/:id`)

#### 功能模块

```
┌─────────────────────────────────────────────────────────┐
│                      歌词编辑                            │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────┬───────────────────────────┐   │
│  │  音频播放器          │  歌词编辑区               │   │
│  │  ┌───────────────┐  │  ┌───────────────────┐   │   │
│  │  │ 波形显示      │  │  │ [00:05.00] 第一句 │   │   │
│  │  │ ▬▬▬▬▬●▬▬▬▬▬  │  │  │ [00:10.50] 第二句 │   │   │
│  │  └───────────────┘  │  │ [00:15.00] 第三句 │   │   │
│  │  [◀] [▶] [⏸] [▶▶]  │  │ [00:20.30] 第四句 │   │   │
│  │                     │  │                   │   │   │
│  │  当前时间: 01:23.45  │  │  [+添加时间戳]    │   │   │
│  └─────────────────────┴───────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  工具栏                                          │   │
│  │  [自动对齐] [导入LRC] [导出LRC] [保存] [重置]   │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

#### 时间戳编辑操作

1. **手动标记**: 播放中按空格键标记当前时间
2. **自动对齐**: 调用后端 Silero VAD 自动检测
3. **拖动调整**: 拖动歌词时间轴调整
4. **预览效果**: 播放时高亮当前歌词

#### API 交互

| 操作 | API | 方法 | 说明 |
|------|-----|------|------|
| 获取歌词 | `/api/v1/lyrics/{song_id}` | GET | 歌词列表 |
| 保存歌词 | `/api/v1/lyrics/{song_id}` | PUT | 更新歌词 |
| 自动对齐 | `/api/v1/lyrics/{song_id}/align` | POST | 触发自动对齐 |
| 获取对齐进度 | WebSocket | - | 实时进度推送 |

---

### 2.4 克隆任务 (`/clone`)

#### 功能模块

```
┌─────────────────────────────────────────────────────────┐
│                      克隆任务                            │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  新建任务                                        │   │
│  │  ┌───────────────────────────────────────────┐  │   │
│  │  │ 选择源歌曲: [下拉选择 ▼]                   │  │   │
│  │  ├───────────────────────────────────────────┤  │   │
│  │  │ 选择音色:   [音色列表 - 卡片选择]          │  │   │
│  │  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐        │  │   │
│  │  │  │🎤A  │ │🎤B  │ │🎤C  │ │+添加│        │  │   │
│  │  │  │流行 │ │民谣 │ │摇滚 │ │新音色│        │  │   │
│  │  │  └─────┘ └─────┘ └─────┘ └─────┘        │  │   │
│  │  ├───────────────────────────────────────────┤  │   │
│  │  │ 音高偏移: [-12 ▬▬▬▬●▬▬▬▬ +12] (半音)     │  │   │
│  │  ├───────────────────────────────────────────┤  │   │
│  │  │ API选择:  ○ Replicate (推荐)              │  │   │
│  │  │          ○ ElevenLabs                     │  │   │
│  │  │          ○ 自定义API Key                   │  │   │
│  │  └───────────────────────────────────────────┘  │   │
│  │                                                  │   │
│  │              [创建任务]                          │   │
│  └─────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  任务列表                                        │   │
│  │  ┌───────────────────────────────────────────┐  │   │
│  │  │ 任务 #123 - 歌曲A → 音色B                  │  │   │
│  │  │ 进度: ████████░░░░░░░░ 60% - 音色转换中    │  │   │
│  │  │ 预计剩余: 2分钟                            │  │   │
│  │  └───────────────────────────────────────────┘  │   │
│  │  ┌───────────────────────────────────────────┐  │   │
│  │  │ 任务 #122 - 歌曲C → 音色A          [已完成]│  │   │
│  │  │                     [▶播放] [⬇下载]       │  │   │
│  │  └───────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

#### 任务状态

| 状态 | 说明 | 进度 |
|------|------|------|
| pending | 等待处理 | 0% |
| separating | 人声分离中 | 10-30% |
| aligning | 歌词对齐中 | 30-40% |
| cloning | 音色克隆中 | 40-80% |
| mixing | 音频混合中 | 80-95% |
| completed | 已完成 | 100% |
| failed | 失败 | - |

#### API 交互

| 操作 | API | 方法 | 说明 |
|------|-----|------|------|
| 创建任务 | `/api/v1/clone/tasks` | POST | 创建克隆任务 |
| 获取任务列表 | `/api/v1/clone/tasks` | GET | 分页查询 |
| 获取任务详情 | `/api/v1/clone/tasks/{id}` | GET | 单个任务信息 |
| 取消任务 | `/api/v1/clone/tasks/{id}/cancel` | POST | 取消进行中的任务 |
| 任务进度推送 | WebSocket `/ws/tasks/{id}` | - | 实时进度 |

---

### 2.5 结果展示 (`/result/:id`)

#### 功能模块

```
┌─────────────────────────────────────────────────────────┐
│                      结果展示                            │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  对比播放器                                      │   │
│  │  ┌───────────────────┬───────────────────────┐  │   │
│  │  │  原始歌曲          │  克隆歌曲             │  │   │
│  │  │  ┌───────────────┐│  ┌───────────────┐   │  │   │
│  │  │  │ 🦞 小龙虾动画 ││  │ 🦞 小龙虾动画 │   │  │   │
│  │  │  └───────────────┘│  └───────────────┘   │  │   │
│  │  │  [▶] ━━━●━━━ 🔊  │  [▶] ━━━●━━━ 🔊    │  │   │
│  │  └───────────────────┴───────────────────────┘  │   │
│  │                                                  │   │
│  │  [同步播放] 两个播放器同步播放对比               │   │
│  └─────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  任务信息                                        │   │
│  │  源歌曲: 歌曲A (3:45)                           │   │
│  │  目标音色: 音色B                                 │   │
│  │  音高偏移: +2 半音                               │   │
│  │  创建时间: 2026-03-18 10:30                      │   │
│  │  处理耗时: 3分12秒                               │   │
│  └─────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  [⬇ 下载MP3] [⬇ 下载WAV] [分享链接] [删除]     │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

#### API 交互

| 操作 | API | 方法 | 说明 |
|------|-----|------|------|
| 获取结果 | `/api/v1/clone/results/{id}` | GET | 结果详情 |
| 下载文件 | `/api/v1/clone/results/{id}/download` | GET | 文件下载 |
| 删除结果 | `/api/v1/clone/results/{id}` | DELETE | 删除结果 |

---

### 2.6 音色管理 (`/voices`)

#### 功能模块

```
┌─────────────────────────────────────────────────────────┐
│                      音色管理                            │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  [➕ 添加音色]  [🔍 搜索]                        │   │
│  └─────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  音色列表                                        │   │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐     │   │
│  │  │ 🎤 流行男声 │ │ 🎤 民谣女声 │ │ 🎤 摇滚男声 │     │   │
│  │  │           │ │           │ │           │     │   │
│  │  │ [▶试听]   │ │ [▶试听]   │ │ [▶试听]   │     │   │
│  │  │ [📝编辑]  │ │ [📝编辑]  │ │ [📝编辑]  │     │   │
│  │  │ [🗑️删除]  │ │ [🗑️删除]  │ │ [🗑️删除]  │     │   │
│  │  └───────────┘ └───────────┘ └───────────┘     │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

#### 添加音色弹窗

```
┌─────────────────────────────────────────┐
│  添加音色                          [×]  │
├─────────────────────────────────────────┤
│  方式选择:                              │
│  ○ 使用预设音色 (推荐)                  │
│  ○ 上传 RVC 模型文件 (.pth)             │
│  ○ 提供外部音色模型 URL                 │
├─────────────────────────────────────────┤
│  音色信息:                              │
│  名称:   [________________________]     │
│  分类:   [流行/民谣/摇滚/其他 ▼]        │
│  描述:   [________________________]     │
│                                         │
│         [取消]  [添加]                  │
└─────────────────────────────────────────┘
```

#### API 交互

| 操作 | API | 方法 | 说明 |
|------|-----|------|------|
| 获取音色列表 | `/api/v1/voices` | GET | 分页查询 |
| 添加音色 | `/api/v1/voices` | POST | 添加音色 |
| 获取试听音频 | `/api/v1/voices/{id}/preview` | GET | 试听音频 |
| 删除音色 | `/api/v1/voices/{id}` | DELETE | 删除音色 |

---

## 3. 公共组件

### 3.1 小龙虾播放器组件 (`LobsterPlayer.vue`)

```vue
<template>
  <div class="lobster-player">
    <!-- 歌词区域 -->
    <LyricsScroll :lyrics="lyrics" :current-index="currentIndex" />
    
    <!-- 小龙虾动画 -->
    <LobsterAnimation :is-singing="isPlaying" :beat-intensity="beatIntensity" />
    
    <!-- 控制栏 -->
    <AudioControls 
      :is-playing="isPlaying"
      :current-time="currentTime"
      :duration="duration"
      :volume="volume"
      @play="play"
      @pause="pause"
      @seek="seek"
      @volume-change="setVolume"
    />
  </div>
</template>
```

### 3.2 文件上传组件 (`FileUploader.vue`)

```vue
<template>
  <div class="file-uploader" 
       @dragover.prevent 
       @drop.prevent="handleDrop">
    <div class="drop-zone" v-if="!file">
      <input type="file" ref="fileInput" @change="handleSelect" hidden />
      <button @click="$refs.fileInput.click()">
        拖拽文件或点击选择
      </button>
      <p class="hint">支持 {{ accept }} 格式，最大 {{ maxSize }}MB</p>
    </div>
    <div class="file-info" v-else>
      <span class="filename">{{ file.name }}</span>
      <span class="size">{{ formatSize(file.size) }}</span>
      <button @click="remove">移除</button>
    </div>
  </div>
</template>
```

### 3.3 任务进度组件 (`TaskProgress.vue`)

```vue
<template>
  <div class="task-progress">
    <div class="progress-bar">
      <div class="fill" :style="{ width: progress + '%' }"></div>
    </div>
    <div class="status">
      <span class="text">{{ statusText }}</span>
      <span class="percent">{{ progress }}%</span>
    </div>
    <div class="time" v-if="estimatedTime">
      预计剩余: {{ estimatedTime }}
    </div>
  </div>
</template>
```

---

## 4. API 请求封装

### 4.1 Axios 实例配置

```javascript
// src/api/request.js
import axios from 'axios'

const api = axios.create({
  baseURL: '/api/v1',
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器
api.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截器
api.interceptors.response.use(
  response => response.data,
  error => {
    // 统一错误处理
    const message = error.response?.data?.message || '请求失败'
    return Promise.reject(new Error(message))
  }
)

export default api
```

### 4.2 API 模块封装

```javascript
// src/api/songs.js
import api from './request'

export const songsApi = {
  // 获取歌曲列表
  getList(params) {
    return api.get('/songs', { params })
  },
  
  // 上传歌曲
  upload(formData, onProgress) {
    return api.post('/songs', formData, {
      headers: { 'Content-Type': 'multipart/form-data' },
      onUploadProgress: onProgress
    })
  },
  
  // 删除歌曲
  delete(id) {
    return api.delete(`/songs/${id}`)
  },
  
  // 触发人声分离
  separate(id) {
    return api.post(`/songs/${id}/separate`)
  }
}
```

### 4.3 WebSocket 连接

```javascript
// src/api/websocket.js
import { io } from 'socket.io-client'

class WebSocketService {
  constructor() {
    this.socket = null
  }
  
  connect(taskId) {
    this.socket = io(`/ws/tasks/${taskId}`, {
      transports: ['websocket']
    })
    
    this.socket.on('progress', (data) => {
      // 更新进度
      this.onProgress?.(data)
    })
    
    this.socket.on('completed', (data) => {
      // 任务完成
      this.onCompleted?.(data)
    })
    
    this.socket.on('error', (error) => {
      // 错误处理
      this.onError?.(error)
    })
  }
  
  disconnect() {
    this.socket?.disconnect()
  }
}

export default new WebSocketService()
```

---

## 5. 状态管理

### 5.1 Pinia Store 结构

```javascript
// src/stores/songs.js
import { defineStore } from 'pinia'
import { songsApi } from '@/api/songs'

export const useSongsStore = defineStore('songs', {
  state: () => ({
    list: [],
    current: null,
    loading: false,
    pagination: {
      page: 1,
      pageSize: 20,
      total: 0
    }
  }),
  
  actions: {
    async fetchList() {
      this.loading = true
      try {
        const { data, total } = await songsApi.getList(this.pagination)
        this.list = data
        this.pagination.total = total
      } finally {
        this.loading = false
      }
    },
    
    async upload(formData) {
      const song = await songsApi.upload(formData)
      this.list.unshift(song)
      return song
    }
  }
})
```

---

*文档版本: 2026-03-18*